# A simple custom couner

- a reusable web component
- using declarative shadow DOM
- server side rendered with PHP
- interactive via JavaScript

Try:

```shell
php -S localhost:8000
```

Then visit your browser.
